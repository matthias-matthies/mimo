use std::thread;

use ui::{run_input_handle, run_output_handle};

fn main() {
    println!("Jan Tanner??");


    // Spawn all the different Threads
    let io_handle = thread::spawn(move || {
        run_output_handle(

        );
    });

    // UI
    // Automations
    // AI
}
